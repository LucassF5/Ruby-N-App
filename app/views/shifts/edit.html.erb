<% content_for :title, "Editar Plantão" %>

<%= native_form_tag %>
<%= native_navbar_tag("Editar Plantão") do |navbar| %>
  <% navbar.submit_button %>
<% end %>

<h1 class="text-large-title text-on-surface mb-6 native-hidden">Editar Plantão</h1>

<%= render "form", shift: @shift %>

<%= render "shared/link_action", text: "Voltar", url: root_path, html: { class: "native-hidden" } %>
